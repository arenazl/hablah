# netlify.toml en la raíz del monorepo:
# Netlify lo lee por default y aplica antes que el de frontend/.
# Importante: `base = "frontend"` le dice a Netlify que el código vive ahí.

[build]
  base = "frontend"
  publish = "dist"
  command = "npm install && npx vite build"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/api/*"
  to = "https://hablah-api-abcaf6c43a5d.herokuapp.com/api/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# HTML, Service Worker y version.json NUNCA se cachean → la PWA siempre ve el build nuevo.
[[headers]]
  for = "/"
  [headers.values]
    Cache-Control = "no-store, max-age=0"
[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "no-store, max-age=0"
[[headers]]
  for = "/version.json"
  [headers.values]
    Cache-Control = "no-store, max-age=0"
[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-store, max-age=0"
