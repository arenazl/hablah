# Handoff 2026-06-09 — Motor pedagógico kids: pulido, latencia y la deuda de observabilidad

> Doc de cierre de sesión para retomar mañana. Sin adornos: qué se hizo, qué se
> intentó, qué falló, y mis errores. Senior audience.

## TL;DR
1. Se pulió el motor kids A0 (asteriscos, densidad, cámara/Contar, onomatopeyas) y se bajó la latencia **−31%** (thinkingBudget 1024→256). Todo LIVE en Heroku.
2. **PROBLEMA CENTRAL SIN RESOLVER:** el motor sigue produciendo un **desfile de palabras sueltas**, no una clase con contexto/explicación. El alumno simulado se queja en la transcripción. Score 52/100.
3. **DEUDA DE FONDO (error mío):** la observabilidad del prompt + transcripción **no está persistida**. Se logueó a stdout (Heroku efímero, rota a ~1500 líneas). La consigna era grabar cada etapa de la construcción del prompt y la charla **de forma permanente** (DB o Cloudinary). Sin eso no se puede diagnosticar en serio.

---

## 1. Lo que se hizo y quedó LIVE (con evidencia)

| Cambio | Dónde | Commit / config |
|---|---|---|
| Limpiar markdown del coach (`*ARMS*`) antes del cliente/TTS | `gemini_live_engine.py::_clean_coach_text` | 09450fb |
| Instrumentación de latencia por turno (`turn.coach_first_response`: total_ms + aísla thinking) | `gemini_live_engine.py` | 33313be |
| Nudge **AVANCE** (densidad de vocab) | enfoque coach `friend` (DB, 2089 chars) vía `update_enfoque_ninos.py` | 09450fb |
| Regla **"el canal es la palabra"** (no acciones físicas) | enfoque `friend` | d83576b |
| Regla **"cero onomatopeyas"** con justificación + de-dup del riel | enfoque `friend` + `seed_methodology_modules.py` + `update_riel_kids.py` (riel mini/A0, 677 chars) | d83576b |
| Tópico **"Contar del 1 al 10" desactivado** (id=140, is_active=0) | `deactivate_topic_conteo.py` | 4f4b336 |
| Frontera de patas + **test de 1 línea** | `MODELO_APRENDIZAJE_ADAPTATIVO.md` | 09450fb |
| **thinkingBudget 1024→256** | Heroku config (release **v133**) | `GEMINI_THINKING_BUDGET=256` |

Anti-freeze: `GEMINI_ACTIVITY_HANDLING` no está en env → usa el default del código `NO_INTERRUPTION` (`gemini_live_engine.py:339`). Confirmado que **el freeze lo evita NO_INTERRUPTION, no el budget**.

### Datos de latencia (server-side `total_ms`, say→1er audio, 40 turnos por valor)
| | budget 1024 | budget 256 |
|---|---|---|
| promedio | 3.00 s | **2.08 s** |
| piso | 2.07 s | **0.63 s** |
| máx | 5.87 s | **3.47 s** |
| freeze | 0 | 0 |

`had_thinking=false` siempre (no hay thinking textual; el costo era thinking **interno** del budget, invisible en el stream).

---

## 2. El problema central: DESFILE, no clase

**La clase en juego es la de Timi (#574)** — sesión REAL (`status=analyzed`, 6 min, 35 turnos, el nene responde como humano). NOTA: la #573 "colores" (Timo) que figuraba antes acá era una corrida de **mi test de latencia**, NO una clase real — fuera de la evidencia.

**#574** (Timi id=23, "Dibujitos y superhéroes", score **52**, keywords_hit 7/13): el coach desfila MASK→FLY→STAR→GLOVES→SMASH→CAPE, una palabra atrás de otra, cada una "decí X". Es "pizza, dog, bye" con otro disfraz. El alumno se queja explícito:
  - `[21] "no me acuerdo qué era cada cosa, no me ibas explicando"`
  - `[33] "con la cape, pero si no me explicás qué es cape"`
  - `[29] "¿estás ahí?"` (se perdió tanto que creyó que se cortó)

**Causa raíz:** el enfoque ordena `ENSEÑAR = HACELO DECIR` (presentá→repetí→siguiente). El modelo lo ejecuta literal: tira la palabra en media frase y salta a la elicitación, **sin explicar el mundo ni unir palabras**. La unidad de la clase es "una palabra", debería ser "una escena/idea explorada". El enfoque optimiza **cantidad de palabras dichas**, no **comprensión**.

---

## 3. Mis errores (sin vueltas)

1. **Observabilidad efímera.** La consigna era persistir cada etapa de la construcción del prompt + la transcripción. Dejé `PROMPT_CIRCUIT` / `PROMPT_FINAL` en `log.info` → stdout → Heroku (rota). Debí persistir en DB (columna en `sessions`) o Cloudinary desde el principio. Consecuencia: no pude recuperar el flow de #574 y propuse "reconstruir" (parche). **Es la falla de fondo de la sesión.**
2. **El nudge AVANCE empeoró el desfile.** Lo agregué para "no machacar una palabra" y empujó al modelo a **correr más rápido por la lista** → menos contexto → el nene no fija nada. Cambié el prompt sin medir el efecto en una clase real antes de darlo por bueno.
3. **Criterio de calidad mal calibrado.** Validé el motor con criterios que miraban **estructura** (intro, elicita, mezcla ES+EN) y daban 8-9/10. El desfile **pasaba** esos criterios. No medí **sustancia** (contexto, explicación, construcción, que el alumno entienda qué dice).
4. **Confundí un bug que no existe en prod.** Dije que el transcript "guarda solo a Habi" — eso era la #573, que es mi test por `say` (texto): ahí no hay `input_transcription`. En **prod con audio el user SÍ se persiste** (la #574 real tiene los 35 turnos de ambos lados). No es bug de producción; lo afirmé sin verificar.
5. **Foco en analogías.** Respondí con explicaciones de nivel "maestro de escuela" a un interlocutor senior. Debí ir directo a código/datos.
6. **Auto-validación con tests que yo controlo (el más grave).** Validé el motor con tests donde el "alumno" era un Gemini complaciente (charla por texto, nene simulado que sigue el hilo y no se frustra) y me di **80-90/100**. La realidad —#574, micrófono, alumno real— fue **~20/100**. Un alumno real se pierde, se confunde y se queja; el simulado nunca lo iba a mostrar. **Mis tests SOLO valen para lo mecánico (latencia, no-freeze, asteriscos, sostener N turnos); la ÚNICA vara de calidad pedagógica es la sesión REAL persistida.** El 80/100 fue humo. La #573 (mi test) no es evidencia; la #574 (real) sí.

---

## 4. Próximos pasos (mañana, en orden)

### A) Observabilidad persistente (PRIORIDAD 1 — bloquea todo lo demás)
- Persistir **por sesión**: (a) circuito de las 4 patas, (b) prompt final entero, (c) transcript palabra×palabra con timing.
- Implementación DB: columna `sessions.prompt_snapshot` (JSON/LONGTEXT). Escribir en `gemini_live.py::_load_session_context` donde HOY se loguea (líneas 212-213) — es en el setup, 1 vez por sesión, no afecta el loop en vivo.
- Si pesa (prompt ~6-8KB × N sesiones): subir el raw a Cloudinary y guardar la URL en `sessions`. Decidir según volumen.
- Conectar la pantalla del backoffice (`Backoffice.tsx` ya muestra `events` por sesión con fallback a transcript) para leer el snapshot, no logs efímeros.
- (Menor) Que el harness de test por `say` registre el lado del user; en prod (audio) ya se guarda — la #574 lo confirma.

### B) Rediseño del enfoque kids (el problema real)
- Cambiar la unidad: de "hacelo decir N palabras" (desfile) a "**explorá 1 escena/idea a fondo**". Patrón objetivo: explicar el mundo → meter 1-2 palabras EN contexto → que el nene las **use** (no repita) → recién avanzar. Menos palabras, más mundo.
- **Revertir/replantear el nudge AVANCE** (hoy agrava el desfile).
- Tópicos-lista (Colores, Contar) son **atractores de desfile**: reescribir con narrativa real o desactivar.

### C) Criterio de evaluación
- Rehacer el juez/criterios para medir **sustancia** (¿explicó?, ¿unió palabras?, ¿el alumno entendió qué dijo?), no solo estructura.

---

## 5. Estado técnico / cómo retomar

- **Sesiones de referencia:** #574 (Timi id=23, superhéroes, score 52) · #573 (Timo id=14, colores).
- **Diagnóstico:** `heroku run "python scripts/diag_session_full.py 574" -a hablah-api` (patas + transcript). `diag_last_session.py <user>` · `diag_latest.py` (última global) · `diag_prompt_flow.py <sid>` (reconstruye el prompt entero llamando `_load_session_context`). **`diag_prompt_flow.py` y `diag_latest.py` están escritos pero NO commiteados/deployados aún.**
- **Enfoque actual** del coach `friend` (2089 chars) en DB; fuente única: `backend/scripts/update_enfoque_ninos.py`.
- **Riel** mini/A0 (677 chars) en `methodology_modules`; fuente: `seed_methodology_modules.py`, se aplica con `update_riel_kids.py` (UPDATE forzado porque el seed hace skip).
- **Flag del motor:** `COMPOSER_MODES=staged_vocab` (kids A0). Revert: `heroku config:unset COMPOSER_MODES`.
- Commits del día en `main` (origin + heroku al día): 4f4b336 → d83576b → 09450fb → 33313be.

## 6. Lo que se intentó y NO se terminó
- Traer el flow de #574 desde logs → **falló** (buffer rotado). Pivot correcto: persistir (no hecho aún).
- `diag_prompt_flow.py` (reconstrucción) → escrito, **no deployado** (el usuario frenó: usar el sistema de trazas/pantalla existente en vez de reconstruir).
